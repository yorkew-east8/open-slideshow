# open-slideshow 初始化配置（复制为 .env 后修改）
# 幻灯片库目录（容器内绝对路径或相对仓库根）
SLIDES_DIR=user-slides

# 首页端口
HOME_PORT=8080

# Slidev 播放器端口
SLIDEV_PORT=3030

# 默认主题（必须已在 apps/player/package.json 安装：default / seriph）
DEFAULT_THEME=default

# 历史列表最大条数
HISTORY_LIMIT=20
